version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '*/'
  cache:
    paths:
      - node_modules/*/
      - .next/cache/*/
  environment:
    variables:
      PORT: 11000 
      CORS_ORIGIN: *
      MONGODB_URI: mongodb+srv://practise:ankit0202@cluster0.ivux2.mongodb.net