    version: 1
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: build # Or 'public', 'dist', etc., depending on your framework
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*